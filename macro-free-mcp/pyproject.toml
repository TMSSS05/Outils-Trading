[project]
name = "macro-free-mcp"
version = "1.0.0"
description = "Free macroeconomic data MCP server"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]
